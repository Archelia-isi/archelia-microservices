{
  "name": "archelia-microservices",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "start": "npm run start --workspaces --if-present",
    "postinstall": "npm run generate --workspace=@archelia/database && npm run build --workspace=@archelia/database && npm run build --workspace=@archelia/core && npm run build --workspace=@archelia/zucchetti && npm run build --workspace=@archelia/shopify && npm run build --workspace=@archelia/typesense && npm run build --workspace=@archelia/ai"
  }
}
