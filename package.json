{
  "name": "archelia-microservices",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "if [ -n \"$RAILWAY_SERVICE_NAME\" ]; then npm run build --workspace=@archelia/$RAILWAY_SERVICE_NAME --if-present; else npm run build --workspaces --if-present; fi",
    "start": "npm run start --workspaces --if-present",
    "postinstall": "npm run generate --workspace=@archelia/database && npm run generate --workspace=@archelia/b2b-database && npm run build --workspace=@archelia/database && npm run build --workspace=@archelia/core && npm run build --workspace=@archelia/zucchetti && npm run build --workspace=@archelia/shopify && npm run build --workspace=@archelia/typesense && npm run build --workspace=@archelia/ai && npm run build --workspace=@archelia/worker-analytics && npm run build --workspace=@archelia/b2b-database && npm run build --workspace=@archelia/ui-storefront",
    "dummy-deploy": "echo 'force deploy'"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.22.0"
  },
  "overrides": {
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "@types/react": "18.3.12",
    "@types/react-dom": "18.3.1"
  }
}
